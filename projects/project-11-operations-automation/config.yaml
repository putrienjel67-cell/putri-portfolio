input_file: data/incoming/operations_intake.csv
output_dir: data/processed
report_dir: reports

required_fields:
  - record_id
  - client_name
  - email
  - request_type
  - status
  - received_at

allowed_statuses:
  - new
  - in_progress
  - waiting
  - completed

text_fields:
  - client_name
  - request_type
  - status
